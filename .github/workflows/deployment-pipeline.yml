name: deployment-pipeline

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  commit:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v5
      - uses: leafo/gh-actions-lua@v10
        with:
          luaVersion: '5.4'
      - uses: leafo/gh-actions-luarocks@v4
      - id: install-deps
        run: make install-deps
      - id: test
        run: make test
      - id: install-hammerspoon
        run: brew install hammerspoon --cask --no-quarantine
      - id: enable-hs-cli
        run: mkdir -p ~/.hammerspoon && echo "hs.ipc.cliInstall()" > ~/.hammerspoon/init.lua
      - id: start-hammerspoon
        run: open /Applications/Hammerspoon.app && sleep 5
      - id: create-package
        run: make build
      - id: verify-artifact
        run: |
          test -f release/ToggleMenubar.spoon.zip
          unzip -t release/ToggleMenubar.spoon.zip
      - id: upload-artifact
        uses: actions/upload-artifact@v5
        with:
          name: release-candidate
          path: release/ToggleMenubar.spoon.zip

  acceptance:
    needs: [commit]
    runs-on: macos-latest
    steps:
      - uses: actions/download-artifact@v5
        with:
          name: release-candidate
      - id: install-hammerspoon
        run: brew install hammerspoon --cask --no-quarantine
      - id: enable-hs-cli
        run: mkdir -p ~/.hammerspoon && echo "hs.ipc.cliInstall()" > ~/.hammerspoon/init.lua
      - id: extract-spoon
        run: |
          mkdir -p ~/.hammerspoon/Spoons
          unzip ToggleMenubar.spoon.zip -d ~/.hammerspoon/Spoons/
      - id: start-hammerspoon
        run: open /Applications/Hammerspoon.app && sleep 5
      - id: wait-for-cli
        run: |
          for i in {1..30}; do
            if command -v hs &> /dev/null; then
              echo "CLI ready"
              exit 0
            fi
            sleep 1
          done
          echo "CLI installation timeout"
          exit 1
      - id: test-spoon-load
        run: hs -c "hs.loadSpoon('ToggleMenubar'); print(spoon.ToggleMenubar ~= nil and 'SUCCESS' or 'FAILED')" | grep SUCCESS
      - id: test-toggle-function
        run: |
          mkdir -p screenshots
          hs -c "hs.loadSpoon('ToggleMenubar'); spoon.ToggleMenubar:toggle()" && sleep 1
          screencapture -x screenshots/menubar-hidden.png
          hs -c "spoon.ToggleMenubar:toggle()" && sleep 1
          screencapture -x screenshots/menubar-visible.png
          echo "## Acceptance Test Screenshots" >> $GITHUB_STEP_SUMMARY
          echo "### Menubar Hidden" >> $GITHUB_STEP_SUMMARY
          echo "![Menubar Hidden](screenshots/menubar-hidden.png)" >> $GITHUB_STEP_SUMMARY
          echo "### Menubar Visible" >> $GITHUB_STEP_SUMMARY
          echo "![Menubar Visible](screenshots/menubar-visible.png)" >> $GITHUB_STEP_SUMMARY
      - id: upload-screenshots
        uses: actions/upload-artifact@v5
        with:
          name: acceptance-screenshots
          path: screenshots/
          retention-days: 7

  release:
    needs: [acceptance]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - id: semver
        uses: ietf-tools/semver-action@v1
        with:
          token: ${{ github.token }}
          patchAll: true
      - uses: actions/download-artifact@v5
        with:
          name: release-candidate
      - id: create-tag
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag ${{ steps.semver.outputs.next }}
          git push origin ${{ steps.semver.outputs.next }}
      - id: create-release
        run: gh release create ${{ steps.semver.outputs.next }} ToggleMenubar.spoon.zip --generate-notes
        env:
          GH_TOKEN: ${{ github.token }}
